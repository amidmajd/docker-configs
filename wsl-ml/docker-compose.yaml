version: "3.0"

services:
  nvidia-pytorch:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - ./src:/src
    restart: always
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
